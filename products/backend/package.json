{
  "name": "backend",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "build": "tsc",
    "generate": "drizzle-kit generate",
    "local:migration": "wrangler d1 migrations apply debt-manager --local",
    "remote:migration": "wrangler d1 migrations apply debt-manager --remote --config wrangler.production.jsonc",
    "test": "vitest run --typecheck",
    "coverage": "vitest run --coverage --typecheck",
    "ui": "vitest run --ui --typecheck"
  },
  "dependencies": {
    "drizzle-orm": "catalog:backend",
    "hono": "catalog:backend"
  },
  "devDependencies": {
    "drizzle-kit": "catalog:backend",
    "wrangler": "catalog:backend",
    "@cloudflare/workers-types": "catalog:backend"
  }
}
